import Phaser from 'phaser';

export class LeagueScene extends Phaser.Scene {
  constructor() {
    super({ key: 'LeagueScene' });
  }

  create(): void {
    // TODO: implement league table / fixture screen
  }
}
