import Phaser from 'phaser';

export class TeamMgmtScene extends Phaser.Scene {
  constructor() {
    super({ key: 'TeamMgmtScene' });
  }

  create(): void {
    // TODO: implement team management / transfer screen
  }
}
